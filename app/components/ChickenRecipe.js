import React from "react";
import HorizotalList from "./HorizotalList";

const ChickenRecipe = ({ data }) => {
  return <HorizotalList title="CheckenRecipe" data={data} />;
};

export default ChickenRecipe;
