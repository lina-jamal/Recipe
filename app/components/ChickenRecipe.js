import React from "react";
import HorizotalList from "./list/HorizotalList";

const ChickenRecipe = ({ data }) => {
  return <HorizotalList title="Checken Recipe.." data={data} />;
};

export default ChickenRecipe;
