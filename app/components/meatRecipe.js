import React from "react";
import VerticaList from "./list/VerticaList";

const MeatRecipe = ({ data }) => {
  return <VerticaList title="Meat Recipe.." data={data} />;
};

export default MeatRecipe;
